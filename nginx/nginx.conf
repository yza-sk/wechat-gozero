user  nginx;
worker_processes  auto;

error_log  /var/log/nginx/error.log warn;
pid        /var/run/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    sendfile        on;
    keepalive_timeout 65;

    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    access_log  /var/log/nginx/access.log  main;

    server {
        listen 80;
        server_name ${SERVER_IP};

        # 用户服务 API
        location /user-api/ {
            rewrite ^/user-api/(.*)$ /$1 break;
            proxy_pass http://user-api:10001/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }

        # 群组服务 API
        location /group-api/ {
            rewrite ^/group-api/(.*)$ /$1 break;
            proxy_pass http://group-api:10003/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }

        # 消息服务 API
        location /msg-api/ {
            rewrite ^/msg-api/(.*)$ /$1 break;
            proxy_pass http://msg-api:10002/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }

        # 默认根路径
        location / {
            return 200 "Welcome to Wechat-GoZero Services\n";
        }
    }

}
